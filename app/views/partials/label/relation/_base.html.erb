<div class="relation">
  <h4><%= klass.model_name.human(:count => 2) %></h4>
  <div class="relation-body">
    <%= raw association_listing(
            label.related_labels_for_relation_class(klass)
        ) { |label|
          link_to label.value, label_path(:id => label)
        }
    %>
  </div>
</div>
